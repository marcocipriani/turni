{
  "name": "turni",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server & npm run dev --workspace web && wait",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "migra": "npm run migra --workspace server",
    "seed": "npm run seed --workspace server",
    "avvio": "npm run avvio --workspace server",
    "verifica": "npm run verifica --workspace server",
    "carico": "npm run carico --workspace server"
  },
  "allowScripts": {
    "esbuild@0.18.20": true,
    "esbuild@0.19.12": true,
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true,
    "esbuild@0.25.12": true,
    "fsevents@2.3.3": true
  },
  "devDependencies": {
    "axe-core": "^4.13.0"
  },
  "license": "MIT"
}
